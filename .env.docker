# ==========================================
# Docker 生产环境配置
# ==========================================

# 应用配置
NODE_ENV=production
PORT=3030

# 数据库连接（使用 Docker 服务名）
DATABASE_URL="postgresql://postgres:postgres@postgres:5432/steam_lesson?schema=public"

# NextAuth 认证配置
NEXTAUTH_SECRET="CHANGE_ME_TO_RANDOM_32_CHARS_STRING_IN_PRODUCTION"
NEXTAUTH_URL="http://localhost:3030"
AUTH_TRUST_HOST=true

# DeepSeek AI API
DEEPSEEK_API_KEY="your_deepseek_api_key_here"
DEEPSEEK_MODEL="deepseek-chat"
DEEPSEEK_BASE_URL="https://api.deepseek.com"

# ==========================================
# 部署前必须修改：
# 1. NEXTAUTH_SECRET: 运行 openssl rand -base64 32 生成
# 2. NEXTAUTH_URL: 改为你的域名（如 https://yourdomain.com）
# 3. DEEPSEEK_API_KEY: 填入真实的 API 密钥
# ==========================================
