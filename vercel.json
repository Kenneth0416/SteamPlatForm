{
  "buildCommand": "prisma generate && next build",
  "devCommand": "next dev --port 3030",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "nextjs",
  "outputDirectory": ".next"
}
